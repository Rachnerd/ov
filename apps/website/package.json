{
  "name": "website",
  "version": "1.0.0",
  "scripts": {
    "prod": "npm run build && npx live-server dist --port=3000",
    "start": "vite",
    "build": "tsc && vite build --emptyOutDir",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@ov/style": "*",
    "@ov/ui-components": "*"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "vite": "^5.1.4",
    "vitest": "^4.1.5"
  }
}
